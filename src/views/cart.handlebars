<h1>Carrito</h1>

<ul>
    {{#each products}}
        <li>
            {{this.product.title}} - ${{this.product.price}}
            <br>
            Cantidad: {{this.quantity}}
        </li>
    {{/each}}
</ul>

<a href="/products">Seguir comprando</a>